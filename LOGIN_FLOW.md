# 曲阜师范大学教务系统模拟登录流程文档

## 概述

本文档描述了曲阜师范大学教务系统（强智教务2017）的模拟登录流程，适用于使用其他编程语言复现。

**目标系统**: 曲阜师范大学教务系统
**基础地址**: `http://zhjw.qfnu.edu.cn/jsxsd/`
**适用范围**: 强智教务2017系统（其他学校使用相同系统只需修改域名）

---

## 登录流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                        登录流程时序图                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  客户端                      服务器                              │
│    │                           │                                │
│    │──① 访问首页─────────────>│  获取初始Cookie                │
│    │<──返回首页+Cookie────────│                                │
│    │                           │                                │
│    │──② 请求验证码───────────>│                                │
│    │<──返回验证码图片─────────│                                │
│    │                           │                                │
│    │──③ 识别验证码(本地)──────│  OCR识别                       │
│    │                           │                                │
│    │──④ 提交登录请求─────────>│  POST登录接口                  │
│    │<──返回登录结果───────────│                                │
│    │                           │                                │
│    │──⑤ 访问主页验证─────────>│  验证登录状态                  │
│    │<──返回主页内容───────────│                                │
│    │                           │                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 详细步骤说明

### 第一步：初始化会话并访问首页

**目的**：建立与服务器的会话连接，获取初始Cookie。

**操作说明**：

1. 创建一个会话对象（Session），用于在多个请求之间保持状态
2. 设置通用的HTTP请求头，模拟正常的浏览器访问行为
   - User-Agent：标识浏览器类型和版本
   - Accept：声明接受的内容类型
   - Accept-Language：声明接受的语言
   - Connection：设置为keep-alive保持连接
3. 向教务系统首页发送GET请求：`/jsxsd/`
4. 服务器会在响应头中返回Set-Cookie，通常包含一个名为JSESSIONID的会话标识

**注意事项**：
- 必须使用同一个会话对象完成后续所有请求
- Cookie会由会话对象自动管理，无需手动处理
- 如果首页访问失败，说明网络有问题或服务器不可用

---

### 第二步：获取验证码图片

**目的**：获取当前会话对应的验证码图片。

**操作说明**：

1. 使用同一个会话对象发送GET请求
2. 请求地址：`/jsxsd/verifycode.servlet`
3. 服务器会返回一张验证码图片的二进制数据
4. 将返回的二进制数据保存为图片或直接传递给OCR识别模块

**注意事项**：
- 验证码与当前会话绑定，不同会话获取的验证码不能混用
- 验证码有时效性，获取后应尽快使用
- 图片格式通常为JPEG或PNG

---

### 第三步：识别验证码

**目的**：将验证码图片中的字符识别为文本。

**操作说明**：

1. 获取验证码图片后，使用OCR技术识别其中的字符
2. 推荐使用ddddocr库，这是一个专门用于识别简单验证码的开源OCR工具
3. 将图片数据传入OCR识别函数，获取识别结果文本
4. 验证码通常为4位字母或数字的组合

**验证码识别重试策略**：
- OCR识别不是百分之百准确
- 如果登录时提示验证码错误，需要重新获取验证码并识别
- 建议最多尝试3次

**其他识别方案**：
- 方案一：使用ddddocr本地识别（推荐）
- 方案二：调用第三方OCR在线服务
- 方案三：将验证码显示给用户，由用户手动输入

---

### 第四步：生成Encoded字符串

**目的**：对用户名和密码进行编码，生成登录所需的encoded参数。

**编码规则**：

1. 将用户名转换为UTF-8字节序列
2. 对用户名字节进行Base64编码，得到编码后的用户名
3. 将密码转换为UTF-8字节序列
4. 对密码字节进行Base64编码，得到编码后的密码
5. 将两部分用三个百分号（%%%）连接起来

**编码公式**：

```
encoded = Base64编码(用户名) + "%%%" + Base64编码(密码)
```

**举例说明**：
- 假设用户名为 "admin"，密码为 "123456"
- Base64编码后：用户名变为 "YWRtaW4="，密码变为 "MTIzNDU2"
- 最终encoded字符串为：`YWRtaW4=%%%MTIzNDU2`

---

### 第五步：提交登录请求

**目的**：向服务器发送登录凭证，完成身份验证。

**操作说明**：

1. 使用POST方法发送请求
2. 请求地址：`/jsxsd/xk/LoginToXkLdap`
3. Content-Type设置为：`application/x-www-form-urlencoded`

**请求头设置**：
- Content-Type：application/x-www-form-urlencoded
- User-Agent：浏览器标识
- Origin：`http://zhjw.qfnu.edu.cn`
- Referer：`http://zhjw.qfnu.edu.cn/`

**请求参数（表单数据）**：

| 参数名 | 参数值 | 说明 |
|--------|--------|------|
| userAccount | 空字符串 | 用户账号字段留空 |
| userPassword | 空字符串 | 密码字段留空 |
| RANDOMCODE | 验证码文本 | 第三步识别出的验证码 |
| encoded | 编码字符串 | 第四步生成的encoded |

**特殊说明**：
- userAccount和userPassword两个字段虽然存在，但实际传递的是空值
- 真正的用户凭证通过encoded参数传递
- 这种设计可能是为了增加安全性或兼容旧系统

---

### 第六步：判断登录结果

**目的**：根据服务器返回的内容判断登录是否成功。

**操作说明**：

无论登录成功与否，HTTP状态码通常都是200。需要通过响应内容来判断结果：

**判断逻辑**：

1. **验证码错误**：如果响应内容包含"验证码错误"字样
   - 说明验证码识别不正确
   - 需要返回第二步重新获取验证码
   - 重新执行登录流程

2. **密码错误**：如果响应内容包含"密码错误"字样
   - 说明用户名或密码不正确
   - 终止登录流程
   - 提示用户检查凭证

3. **登录成功**：响应内容不包含上述错误信息
   - 登录成功
   - 继续下一步验证

---

### 第七步：验证登录状态

**目的**：访问需要登录权限的页面，确认登录确实成功。

**操作说明**：

1. 使用同一个会话对象发送GET请求
2. 请求地址：`/jsxsd/framework/xsMain.jsp`
3. 这是教务系统的学生主页，只有登录后才能正常访问

**判断标准**：
- 如果响应状态码为200，且页面内容为学生主页信息
- 则确认登录成功，可以进行后续操作

---

## 完整流程伪代码

```
函数：模拟登录(用户名, 密码)

    第一步：初始化
    创建会话对象
    设置默认请求头（User-Agent、Accept等）

    第二步：访问首页
    发送GET请求到 /jsxsd/
    如果失败，返回错误

    第三步：登录循环（最多3次尝试）

        步骤3.1：获取验证码
        发送GET请求到 /jsxsd/verifycode.servlet
        获取验证码图片数据

        步骤3.2：识别验证码
        使用OCR识别验证码图片
        获取验证码文本

        步骤3.3：生成encoded
        用户名Base64编码
        密码Base64编码
        拼接：encoded = 编码后用户名 + "%%%" + 编码后密码

        步骤3.4：提交登录
        构造表单数据：
            - userAccount = 空
            - userPassword = 空
            - RANDOMCODE = 验证码文本
            - encoded = encoded字符串
        发送POST请求到 /jsxsd/xk/LoginToXkLdap

        步骤3.5：判断结果
        如果响应包含"验证码错误"：
            继续下一次循环（重新获取验证码）
        如果响应包含"密码错误"：
            返回错误：用户名或密码错误
        否则：
            跳出循环，登录成功

    第四步：验证登录状态
    发送GET请求到 /jsxsd/framework/xsMain.jsp
    如果状态码为200：
        返回成功
    否则：
        返回失败

函数结束
```

---

## 关键注意事项

### 1. 会话管理

- 整个登录流程必须使用同一个会话对象
- 会话对象会自动管理Cookie，无需手动处理
- 不要在请求之间关闭或重置会话

### 2. 验证码处理

- 验证码与会话绑定，不能跨会话使用
- 验证码有时效性，建议获取后立即使用
- OCR识别率不是百分之百，必须实现重试机制

### 3. 请求频率

- 避免过于频繁的请求，可能触发反爬机制
- 在重试之间建议添加短暂延迟（约1秒）

### 4. 网络超时

- 设置合理的超时时间（建议10秒左右）
- 处理网络异常，实现重连机制

### 5. 安全考虑

- 不要将用户名密码硬编码在代码中
- 建议使用环境变量或配置文件存储凭证
- 系统使用HTTP协议传输，敏感信息可能被截获

---

## 错误处理清单

| 错误情况 | 可能原因 | 处理方式 |
|---------|---------|---------|
| 首页访问失败 | 网络问题或服务器不可用 | 检查网络连接，稍后重试 |
| 验证码获取失败 | 会话失效或服务器问题 | 重新初始化会话 |
| 验证码识别错误 | OCR识别不准确 | 重新获取验证码并重试 |
| 用户名密码错误 | 凭证不正确 | 提示用户检查输入 |
| 登录请求超时 | 网络延迟 | 增加超时时间或重试 |
| 主页访问失败 | 登录状态失效 | 重新执行登录流程 |

---

## 适配其他学校

如需适配其他使用强智教务2017系统的学校，只需修改域名部分。

**需要修改的地址**：

| 功能 | 原地址 | 新地址 |
|------|--------|--------|
| 首页 | /jsxsd/ | 不变 |
| 验证码 | /jsxsd/verifycode.servlet | 不变 |
| 登录接口 | /jsxsd/xk/LoginToXkLdap | 不变 |
| 主页 | /jsxsd/framework/xsMain.jsp | 不变 |

只需将域名 `zhjw.qfnu.edu.cn` 替换为目标学校的教务系统域名即可。

---

## 核心要点总结

1. **会话保持**：全程使用同一个会话对象
2. **Cookie自动管理**：无需手动处理Cookie
3. **验证码绑定**：验证码与会话一一对应
4. **编码方式**：用户名和密码分别Base64编码后用"%%%"连接
5. **参数传递**：登录时userAccount和userPassword为空，凭证通过encoded传递
6. **错误判断**：通过响应内容包含的关键字判断错误类型
7. **重试机制**：验证码错误可重试，密码错误需终止

---

## 文档信息

- 文档版本：1.0
- 适用系统：强智教务2017
- 基于项目：QFNULogin
